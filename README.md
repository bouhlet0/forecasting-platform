# WIP

## Setup

1. Clone the repo
2. Create a virtual environment: `python -m venv .venv && source .venv/bin/activate`
3. Install dependencies: `pip install -e .`
4. Place M5 raw CSV files in `data/raw/`
5. Run `python scripts/convert_to_parquet.py`
